You need to have dependant libraries installed.
A full list is available in coral.rb.   If a require file is
not found it's likely you need to install the game for it.

sudo gem install <name>

Currently there is little installation process. 
Here's a few things you will want to do:

1. Set up values in constants.rb.
2. Set up values in imcruby.rb. 

